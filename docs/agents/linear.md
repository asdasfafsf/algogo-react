# Linear 운영 규칙

이 문서는 사람과 AI 에이전트 모두가 따르는 Linear 이슈 관리 규칙이다.

## 작업 범위

조회 외 이슈 생성·수정·상태·체크리스트 갱신은 현재 요청에서 승인된 범위에서만 수행한다. Git 커밋·PR·머지 승인을 별도의 Linear 쓰기 승인으로 확대하지 않는다. 코멘트 등 메시지 전송은 사용자의 명시적 전송 요청이 있을 때만 수행한다. 이미 승인된 동일 작업은 다시 확인하지 않는다. 아래 상태 전환과 기록 절차에도 이 범위가 적용된다.

## 프로젝트 설정

- **워크스페이스**: Wonkeun
- **팀**: Algogo (ID: `923777c1-bdf0-4827-b2f5-a36bcfed6cab`)
- **프론트 프로젝트**: [Algogo-react](https://linear.app/wonkeun/project/algogo-react-7385e4146074) (ID: `86afa3bd-c648-4b01-b464-0ce9b9454f25`). 이 저장소의 이슈는 이 프로젝트에 배정하며 백엔드 프로젝트를 대신 사용하지 않는다.
- 작업 전 현재 팀·프로젝트·상태·라벨 ID를 실제 연결에서 확인한다. 아래 팀 공통 ID는 이관 기준이며 변경될 수 있다.
- **이슈 접두어**: ALGOGO (ALGOGO-1, ALGOGO-2...)
- **연동**: 사용 가능한 Linear 연결 도구를 우선 사용한다. 연결되지 않으면 환경변수 `LINEAR_API_KEY`로 GraphQL API (`https://api.linear.app/graphql`)를 호출할 수 있다. 키 값을 출력하거나 파일에 복사하지 않는다.

### 상태 ID

| 상태 | ID |
|------|----|
| Backlog | `13280734-676a-4ae3-be1e-b749410a97b6` |
| Todo | `5e3bed09-30f8-4b4c-be9d-b02fdcfb7176` |
| In Progress | `4b82c7b7-ec3a-4d21-a6a6-03e2e8425d68` |
| In Review | `eb5ddfd8-5075-4679-ab36-6d0c5f5e160e` |
| Done | `77a08e0c-d5fe-40db-9415-d3a41fdbb64a` |
| Canceled | `c43225e8-8719-4e6e-a022-f3122b2ce038` |
| Duplicate | `cc9eaa65-29d9-4cef-ba1b-8f72b3651d39` |

### 라벨 ID

| 라벨 | ID |
|------|----|
| Bug | `98df7788-38cf-437c-ac2a-cb86d71568ef` |
| Feature | `1eac428f-d034-4f32-893a-68b8ab722299` |
| Improvement | `19cabe3a-1292-47d1-b107-7c6e382ddb32` |

---

## 계층 구조

```
Project (기능 단위 묶음, 기한 있음)
  └── Issue (실제 작업 단위)
        └── Sub-issue (이슈를 더 쪼갠 것)
```

### Project

구체적인 기능 묶음. 기한이 있고 완료 조건이 명확하다.

### Issue

실제 작업 단위. 한 사람이 맡아서 완료할 수 있는 크기.

### Sub-issue

이슈가 클 때 하위로 쪼갠 것. 추정치 8 이상이면 반드시 분할한다.

부모 이슈는 모든 Sub-issue가 Done이 되면 Done으로 전환한다.

---

## 이슈 작성 규칙

### 필드 구분

| 필드 | 구분 | 미지정 시 기본 동작 |
|------|------|-------------------|
| 제목 | **필수** | - |
| 라벨 | **필수** | 작업 내용으로 판단; 결정할 수 없을 때만 확인 |
| 설명 | **권장** | 추정치 1~2이고 제목만으로 충분하면 생략 가능 |
| 우선순위 | 선택 | No Priority로 생성. 사이클에 넣을 때 지정 |
| 추정치 | 선택 | 미지정 유지. 사용자가 추정치 지정을 요청한 경우에만 합의 |
| Project | **필수** | 확인된 프론트 프로젝트에 배정 |
| 담당자 | 선택 | 미지정 |

### 제목

한글로 간결하게. 무엇을 하는지 한눈에 보여야 한다.

```
좋은 예:
  로그인 페이지 비밀번호 유효성 검사 추가
  대시보드 차트 렌더링 오류 수정
  문제 목록 페이지네이션 구현

나쁜 예:
  로그인 관련 작업
  기능 추가
  버그 수정
```

### 설명 (유형별 템플릿)

#### Feature (새 기능)

```markdown
## 배경
왜 이 기능이 필요한지.

## 관련
- 상위 이슈: (있으면 링크)
- 설계 문서: (있으면 링크)

## 할 일
- [ ] 구체적인 작업 항목 1
- [ ] 구체적인 작업 항목 2

## 완료 조건
- 이 이슈가 끝났다고 판단할 수 있는 객관적 기준

## 결과
- (완료 시 PR 링크를 여기에 추가)
```

#### Bug (버그)

```markdown
## 재현 경로
1. 어떤 조건에서
2. 무엇을 하면
3. 어떤 문제가 발생하는지

## 기대 동작
- 정상적으로 어떻게 동작해야 하는지

## 실제 동작
- 현재 어떻게 동작하는지

## 관련
- 발견 경위: (어떤 작업 중 발견했는지, 관련 이슈 링크)
- 원인 추정: (있으면)

## 완료 조건
- 재현 경로대로 수행했을 때 기대 동작이 나올 것

## 결과
- (완료 시 PR 링크를 여기에 추가)
```

#### Improvement (개선)

```markdown
## 현재 상태
- 지금 어떻게 동작하고, 무엇이 문제인지

## 개선 방향
- 어떻게 바꾸고 싶은지

## 관련
- 관련 이슈: (있으면 링크)

## 완료 조건
- 개선이 적용되었다고 판단할 수 있는 기준

## 결과
- (완료 시 PR 링크를 여기에 추가)
```

#### 공통 규칙

- **관련** 섹션: 이 작업이 어디서 파생됐는지, 어떤 이슈와 연결되는지를 기록한다. 독립적인 이슈면 생략 가능.
- **결과** 섹션: 이슈를 Done으로 옮길 때 PR 링크를 추가하여 작업 결과물을 추적한다.

### 라벨

| 라벨 | 사용 시점 |
|------|----------|
| Feature | 없던 기능을 새로 추가 |
| Bug | 기존에 동작하던 것이 깨진 경우 |
| Improvement | 동작은 하지만 더 나아지길 원하는 것 (리팩터링, 성능 개선, 문서 정리, 테스트 추가 등) |

하나의 이슈에 라벨은 하나만 붙인다.

**판단 기준**: 기존 기능의 동작 방식을 바꾸거나 확장하면 Improvement, 아예 없던 것을 만들면 Feature. chore, docs, test 류 작업은 Improvement로 분류한다.

### 우선순위

| 단계 | 기준 |
|------|------|
| Urgent | 다른 작업을 블로킹. 당장 해결해야 함 |
| High | 현재 프로젝트의 핵심 기능 |
| Medium | 현재 프로젝트에 포함되지만 핵심 경로는 아닌 것 |
| Low | 있으면 좋지만 미뤄도 되는 것 |
| No Priority | Backlog에 넣어둔 아이디어. 사이클에 넣을 때 반드시 우선순위를 지정한다 |

### 크기 추정 (Estimate)

| 포인트 | 기준 |
|--------|------|
| 1 | 단순 수정. 한 파일, 명확한 변경 |
| 2 | 작은 기능. 1~2개 파일 |
| 3 | 보통 기능. 여러 파일, 약간의 설계 필요 |
| 5 | 큰 기능. 여러 모듈에 걸침 |
| 8 | 매우 큰 기능. 반드시 Sub-issue로 분할한다 |

---

## 상태 전환

```
Backlog → Todo → In Progress → In Review → Done
                       ↑            │
                       └────────────┘ (문제 발견 시)
```

| 전환 | 시점 |
|------|------|
| → Todo | 사이클에 포함되거나 작업 범위에 합의됨 |
| → In Progress | 코드 작성 시작. 생성 직후 바로 작업하면 In Progress로 바로 생성 |
| → In Review | PR 생성 |
| → Done | 리뷰 통과, 테스트 확인, 머지 완료. 결과 섹션에 PR 링크 추가 |
| → Canceled | 작업이 불필요해짐. 사유를 코멘트로 남긴다 |
| → Duplicate | 중복 이슈. 원본 이슈를 코멘트에 링크한다 |

### 할일 체크 규칙

이슈 설명의 체크리스트(`- [ ]`)는 작업 완료 시 한꺼번에 체크하지 않는다. 각 항목을 완료할 때마다 즉시 `- [x]`로 업데이트한다.

### 코멘트 규칙

다음 상황에서는 이슈에 코멘트를 남겨 맥락을 기록한다:

- 블로커를 발견했을 때
- 스펙이 변경되었을 때
- 상태를 되돌릴 때
- 오래(3일 이상) In Progress 상태일 때

---

## AI 에이전트 이슈 조회 규칙

이슈를 조회할 때는 **반드시 확인된 프론트 프로젝트 필터를 적용**한다. 팀에 여러 프로젝트가 존재하므로, 프로젝트 필터 없이 조회하면 다른 프로젝트의 이슈가 섞여 혼란을 일으킨다.

```graphql
# 좋은 예 — 확인된 프론트 프로젝트 ID를 $projectId 변수로 전달
issues(filter: {
  state: { type: { nin: ["completed", "canceled"] } },
  project: { id: { eq: $projectId } }
})

# 나쁜 예 — 프로젝트 필터 없음
issues(filter: {
  state: { type: { nin: ["completed", "canceled"] } }
})
```

목록·검색·중복 검사는 프로젝트 필터를 적용한다. 단일 이슈 ID 조회처럼 필터를 지원하지 않는 API는 응답의 프로젝트 소속을 확인한다.

---

## AI 에이전트 이슈 생성 플로우

### 생성 절차

```
1. 사용자 발화에서 이슈 생성 의도 감지
2. 같은 프로젝트의 활성 이슈를 확인하고 명확한 기존 이슈가 있으면 연결; 모호한 경우만 확인
3. 작업 내용으로 유형(라벨) 결정; 결과에 중요한 모호함이 남으면 확인
4. 추정치가 8 이상으로 판단되면 Sub-issue 분할을 사용자에게 제안
5. 생성이 이미 승인되었으면 작성한 제목·유형·설명으로 진행; 승인이 없으면 구체적인 초안을 보여주고 생성 여부 확인
6. 사용 가능한 Linear 연결 도구로 생성; 연결 도구를 사용할 수 없고 API 접근이 가능하면 LINEAR_API_KEY로 GraphQL API 호출
7. 생성된 이슈 번호와 URL을 사용자에게 전달
```

### 필드 처리 규칙

- **제목, 라벨**: 반드시 채운다. 판단이 어려우면 사용자에게 확인.
- **우선순위**: 사용자가 언급하지 않으면 No Priority로 생성.
- **추정치**: 사용자가 언급하지 않으면 미지정으로 생성.
- **설명**: 대화 내용에서 핵심만 뽑아 해당 유형의 템플릿에 맞춰 작성.
- **Project**: 확인된 프론트 프로젝트에 배정한다.

### 상태 관리 규칙

- 사용자가 "지금 하자", "바로 시작" 등 명시적으로 즉시 작업을 요청한 경우 → In Progress로 생성.
- 그 외 → Backlog로 생성한다.
- PR 생성 시 → In Review로 전환한다. 셀프 리뷰로 바로 머지하는 경우 In Review를 건너뛰어도 된다.
- 머지 완료 시 → Done으로 전환하고 결과 섹션에 PR 링크를 추가한다.

### 금지 사항

- 사용자 동의 없이 임의로 이슈를 생성하지 않는다.
- 추정치와 우선순위를 임의로 정하지 않는다.
- 모든 변경 작업의 브랜치명·커밋·PR에 실제 Linear 이슈 번호를 포함한다 (Git 규칙 참조).
- 코드·문서·설정 변경 전 반드시 이슈를 연결하고 별도 브랜치에서 진행한다. 로컬 작업이라는 이유로 생략하지 않는다. 이슈 확인을 위한 읽기 전용 조사는 가능하다. 이슈를 임의로 생성하거나 번호를 지어내지 않는다.
