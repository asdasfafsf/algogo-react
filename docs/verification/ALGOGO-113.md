# ALGOGO-113 오늘의 문제 화면 검증

fixture: `/tests/fixtures/today-problem.html`

## 확인 항목

| 대상      | 확인 방법                               | 기대 결과                                                          |
| --------- | --------------------------------------- | ------------------------------------------------------------------ |
| 날짜 이동 | 이전·다음 버튼과 날짜 선택으로 이동     | `day` 쿼리가 -30~0 범위에서 바뀌고 오늘 이후로는 이동하지 않음     |
| 오늘 복귀 | 지난 날짜에서 `오늘로 이동` 선택        | `day` 쿼리가 제거되고 오늘 날짜로 복귀                             |
| 문제 선택 | 진행 표시와 목록 행을 각각 선택         | 선택한 문제의 카드와 목록 강조가 함께 바뀜                         |
| 문제 상세 | 문제 카드의 `문제 보기` 선택            | 기존 문제 상세 주소를 새 창으로 열 수 있음                         |
| 빈 화면   | fixture에서 `empty` 선택                | 날짜 탐색을 유지한 채 다른 날짜와 전체 문제로 갈 수 있는 안내 노출 |
| 오류 화면 | fixture에서 `error` 후 `다시 시도` 선택 | 오류 원인을 숨기지 않고 재조회 행동 제공                           |
| 로딩 화면 | fixture에서 `loading` 선택              | 실제 화면 구조에 맞는 골격 화면 노출                               |
| 모바일    | 390px 너비에서 위 항목 확인             | 날짜 이동, 진행 표시, 카드와 목록이 가로 넘침 없이 표시            |

## 실행 결과

| 대상         | 결과                                                                                                   |
| ------------ | ------------------------------------------------------------------------------------------------------ |
| 기본 데이터  | 맞힌·실패·미풀이 문제 3개와 각각의 문제 요약, 난이도·유형·풀이 정보를 확인                             |
| 문제 선택    | 진행 표시에서 두 번째 문제를 고른 뒤, 목록에서 세 번째 문제를 골라도 카드와 목록 선택 표시가 함께 바뀜 |
| 날짜 이동    | 전날 이동 후 다음 날짜와 `오늘로 이동`으로 오늘에 복귀하는 흐름 확인                                   |
| 날짜 범위    | 날짜 입력으로 30일 전으로 이동했을 때 이전 날짜 버튼이 비활성화되고, `오늘로 이동`으로 복귀함          |
| 상세 이동    | 카드의 `문제 보기`가 `/problem/today-sample-1` 새 탭을 열고 해당 문제 작업 공간으로 이동함             |
| 빈·오류·로딩 | 빈 안내의 전체 문제 링크, 오류 후 `다시 시도` 성공 복귀, 화면 구조를 반영한 로딩 골격을 확인           |
| 모바일       | 390×844에서 카드와 목록이 가로로 넘치지 않으며, 긴 문제 제목은 필요한 범위에서 줄임표로 표시됨         |
