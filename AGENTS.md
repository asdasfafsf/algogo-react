# Algogo React 에이전트 지침

이 문서는 저장소 전체에 적용되는 공통 지침이다. 사용자 지시와 실행 환경의 상위 지침을 우선한다. 설명과 작업 보고는 한글로 작성하고, 코드 식별자와 명령어는 원문을 유지한다.

## 작업 원칙

- 요청의 목표·제약·완료 조건을 파악하고, 승인된 범위에서 구현과 검증까지 진행한다. 계획이나 제안만 남기고 멈추지 않는다.
- 쉽게 되돌릴 수 있는 구현 선택은 근거를 확인해 직접 결정한다. 답이 결과를 크게 바꾸거나 필요한 권한이 없을 때만 짧게 질문한다. 이미 받은 승인을 반복해서 묻지 않는다.
- 코드·설정·테스트·실행 결과를 근거로 판단한다. 버전과 도구 이름은 현재 워크트리에서 확인하고 특정 모델 이름이나 과거 실행 환경에 고정하지 않는다.
- 작업 전 브랜치·변경 사항을 확인한다. 기존 작업을 이어가고, 사용자나 다른 작업의 변경을 덮어쓰지 않는다. 범위 밖 리팩터링을 섞지 않는다.
- 독립적인 조회는 묶고, 필요한 파일과 문서부터 읽는다. 복잡한 작업만 짧은 계획으로 관리한다. 관련 없는 전체 문서를 매번 읽지 않는다.
- 모든 셸 명령에 `rtk`를 붙인다. 전용 명령이 없으면 `rtk proxy <명령>`을 사용한다. 연결한 명령 각각에도 적용한다.
- MoAI 오케스트레이션을 사용하지 않는다. 위임은 사용자 요청이나 적용 지침이 있을 때 독립적인 하위 작업에 사용하고, 워크트리와 파일 소유 범위를 명시한다.
- 설명과 보고는 한글로 간결하게 작성한다. 완료한 변경, 검증 결과, 남은 문제를 구분하고 실행하지 않은 검증을 통과했다고 보고하지 않는다.

## 상세 규칙 읽기

아래 문서는 이 지침의 일부다. 링크만 확인하고 넘어가지 말고, 해당 작업을 시작하기 전에 본문을 읽는다. Claude의 `paths` 자동 로딩에 의존하지 않는다.

| 작업 범위 | 읽을 문서 |
|-----------|-----------|
| TypeScript/React: `src/**/*.{ts,tsx}`, 스타일, `tsconfig*.json`, `vite.config.ts`, `eslint.config.js` | [코딩 규칙](docs/agents/code.md) |
| 브랜치·커밋·PR·머지 | [Git 규칙](docs/agents/git.md) |
| 이슈 조회·작성·상태 관리 | [Linear 규칙](docs/agents/linear.md) |
| 이슈부터 완료까지 작업 진행 | [작업 절차](docs/agents/work.md) |
| 에이전트 지침 문서 수정 | [지침 관리](docs/agents/workflow.md) |

## 개발과 검증

React + TypeScript + Vite 프론트엔드이며 Tailwind CSS, React Router, Zustand, TanStack Query, Axios, Monaco, Socket.IO를 사용한다. 실행 버전은 현재 워크트리의 `package.json`, `.nvmrc`, 잠금 파일을 확인한다.

- 의존성 설치: `rtk pnpm install --frozen-lockfile`
- 개발 서버: `rtk pnpm dev`
- 타입 검사 및 프로덕션 빌드: `rtk pnpm build`
- 린트: `rtk pnpm lint`
- 수정한 JS/TS 파일: `rtk pnpm exec eslint --fix <파일>` 및 `rtk pnpm exec prettier --write <파일>`
- 수정한 JSON/CSS 파일: `rtk pnpm exec prettier --write <파일>`

이관 시점에는 별도 test/E2E 스크립트가 없다. 테스트 명령을 지어내거나 실행하지 않은 테스트를 통과로 보고하지 않는다. 현재 스크립트를 확인하고, 변경에 맞는 회귀 테스트 또는 브라우저 검증을 수행한다. UI·라우팅·상태 관리·에디터 변경은 실제 로컬 화면에서 관련 동작을 확인한다. 백엔드와 인증이 필요한 동작은 샘플/mock 검증과 실제 연동 검증을 구분한다.

워크트리에는 `.env`가 자동으로 복사되지 않는다. API 대상과 개발 포트를 확인하고, 비밀값을 출력하거나 커밋하지 않는다. 사용자에게 보여줄 주소는 실제 실행 중인 서버로 안내한다.

## 작업 흐름과 보호 규칙

모든 저장소 변경은 Linear 이슈 → 이슈 번호를 포함한 별도 워크트리 브랜치 → 구현 → 검증 → 커밋 → PR → 머지 → Done 순서다. 문서·설정·로컬 수정도 예외가 아니다. 이슈 확인을 위한 읽기 전용 조사와 변경 검토는 먼저 수행할 수 있다. 상세 절차는 [작업 절차](docs/agents/work.md)를 따른다. 현재 요청에서 승인된 작업 범위에 한해 진행한다.

- 기능 작업은 별도 워크트리에서 수행한다. 이미 지정된 작업 워크트리가 있으면 이어서 사용하고, 기본 작업 디렉토리의 브랜치를 임의로 전환하지 않는다.
- `main`에 직접 커밋·push·로컬 머지를 하지 않는다. `dev`에도 직접 push하지 않는다.
- 대안을 검토하고 선택한 결정 중 코드만으로 이유가 드러나지 않는 것은 [ADR 지침](docs/adr/README.md)에 따라 기록한다.
- Claude의 훅은 다른 에이전트에서 자동 실행되지 않는다. 위 브랜치 보호, 파일별 lint/format, ADR 확인을 직접 수행한다. 이 문서는 자동 차단 장치를 설치하지 않는다.

## Claude 문서와의 관계

공통 지침의 기준은 이 파일과 `docs/agents/`다. `CLAUDE.md`는 이 공통 지침을 참조한다. 백엔드의 `.claude/` 설정이나 훅은 이 프론트 저장소에 복사하지 않는다. 실제 Claude 전용 설정이 추가되면 공통 지침과 일관성을 확인한다.

## 프로젝트 로컬 스킬

- [algogo-work](.agents/skills/algogo-work/SKILL.md): 이 프론트 저장소에서 Linear 이슈 기반 작업을 시작하거나 이어갈 때 읽고 사용한다. Codex 호출 예: `$algogo-work ALGOGO-번호 작업을 진행해줘`.
- 스킬은 `.agents/skills/`에서 관리한다. Claude 전용 `/work` 명령은 별도로 설치하지 않으며, 사용 중인 도구에서 지원하는 방식으로 같은 스킬을 읽는다.

## 완료 기준

요청한 동작을 구현하고 변경 위험에 맞는 검증을 통과해야 구현 완료다. 문서만 바꾸면 링크·내용 일관성·차이 검사를 수행하며 앱 전체 테스트는 생략할 수 있다. 의존성·타입·런타임을 바꾸면 타입 검사·빌드·관련 테스트를 수행한다. 외부 서비스 동작은 mock 검증만으로 정상 동작을 단정하지 않는다.

검증이 통과하면 새 변경이나 실패 근거 없이 같은 검사를 반복하지 않는다. 커밋·PR·머지·배포 여부는 구현 완료와 별도로 보고하며, 외부 반영은 요청에서 승인된 범위 안에서 수행한다.
